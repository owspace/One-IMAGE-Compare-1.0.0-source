# One IMAGE Compare 开发手稿

> 用途：给产品、设计、开发以及新电脑上的 Codex 提供一份可持续维护的工程交接文档。
>
> 当前代码基线：main 分支，初始源码提交 5bd5da9
>
> GitHub 仓库：https://github.com/owspace/One-IMAGE-Compare-1.0.0-source
>
> 文档更新时间：2026-08-13

## 0. 新 Codex 接手时先读什么

新电脑首次打开工程时，先按下面顺序建立上下文：

1. 阅读本文件，了解产品决策、历史方案和未完成风险。
2. 阅读 README.md，确认当前构建、运行和 DMG 生成入口。
3. 重点阅读 Sources/OneImageCompare/Models.swift、ImagePipeline.swift、PhotoStore.swift、ContentView.swift、ImageCanvas.swift、ViewportState.swift、HistogramView.swift、EffectAnalysisViews.swift。
4. 先运行结构校验和 Debug 构建，再修改代码：

    ./scripts/verify_structure.sh
    swift build -c debug

5. 修改前先确认当前行为属于以下哪一类：图片源数据解析、显示渲染、分析统计、交互状态，还是打包发布。不要把显示结果、分析数据和元数据解析混在同一个修复中。

推荐给新 Codex 的第一条指令：

    请先阅读仓库中的 开发手稿.md 和 README.md，再检查当前 Git 状态、运行结构校验和 swift build -c debug。先给出与手稿一致的现状审计和待办优先级，未经确认不要重写整体架构。

## 1. 产品目标与当前定位

One IMAGE Compare 是一个面向 macOS 的图片浏览、选图、单图查看、多图对比和 HDR/Gain Map 技术分析工具。它不是普通的图片查看器，也不是完整的照片编辑器，核心价值是：

- 快速浏览一组照片，并用接近 XnView 的方式进行选择、定位和对比。
- 在 SDR、HDR 两种显示模式之间切换，观察同一张照片在不同显示路径中的结果。
- 识别并展示 Apple/ISO 21496-1 Gain Map 的技术信息。
- 将照片本身的 SDR 基础图、Gain Map 以及 HDR 相关元数据分开分析。
- 对 SDR 和 HDR 的影调、色彩、动态范围、细节和肤色提供可解释的客观指标。
- 为工程、竞品分析和显示效果验证提供可复现的分析工具，而不是只给出主观描述。

当前工程采用 Swift Package Manager 组织源码，应用层使用 AppKit、SwiftUI 和 Metal 相关能力，图像路径使用 ImageIO、Core Image、Core Graphics，面部检测使用 Vision，辅助信息和工程项目文件使用 PDFKit/系统文件能力。当前目标为 macOS 14+、Apple Silicon arm64；已有内部可安装的 arm64 DMG 构建脚本，但目前仍属于开发/内部验证分发，不等同于 Developer ID 签名和公证发布。

当前已经形成的功能闭环包括：

- 文件夹图片浏览与缩略图网格。
- 单图查看和多图对比。
- Shift 连选范围、Enter 进入对比、B 切换 SDR/HDR。
- 缩略图切换时不自动跳入大图。
- SDR/HDR 图片缓存和异步分析。
- ImageIO 原生 SDR/HDR 解码路径。
- ISO 21496-1 Gain Map 解析和 Core Image auxiliaryHDRGainMap 兼容路径。
- Gain Map API、Headroom、色彩空间、传输函数、像素格式、尺寸和权重等信息展示。
- 可折叠直方图。
- SDR 效果分析、HDR 效果分析和肤色/人脸分析。
- 工程保存与打开的基础能力。
- arm64 DMG 生成、结构校验和 GitHub Actions 构建入口。

## 2. 开发全过程与关键决策

### 2.1 工程修复、构建和交付基础

最初目标是检查原有 macOS 工程、修复编译错误、完成运行验证并生成 arm64 DMG。过程中补齐了 SwiftPM 工程入口、资源清单、Info.plist、构建脚本和结构校验脚本，并将构建过程整理为可重复执行的命令。

当前主要入口：

- Package.swift：SwiftPM 工程和平台/目标配置。
- scripts/verify_structure.sh：检查必须的源码、资源、脚本和目录结构。
- scripts/build_dmg.sh：构建 arm64 应用并生成 DMG。
- .github/workflows/build-macos.yml：在 GitHub Actions 中进行 macOS 构建验证。

工程验证原则已经从“能否在当前机器打开”转为“源码、脚本、资源和发布产物都能被检查”。DMG 不直接提交到 Git，因为它属于可再生构建产物，仓库通过脚本重新生成。

### 2.2 HDR 转 SDR 的慢速问题

最初的思路是由应用自己读取 Gain Map，然后对 HDR/SDR 做手工映射。这个方案可以控制公式，但存在三个问题：

1. 每次切换都可能重新创建大尺寸像素缓冲区。
2. 颜色空间、传输函数、Gain Map 权重和 Headroom 处理稍有偏差，结果就不再等同于系统显示路径。
3. SDR 和 HDR 的显示、分析、直方图如果共用同一条转换链，切换时容易重复解码。

后来确认，单纯“屏蔽 Gain Map”可以得到基础 SDR 图，但它不等于完整的 HDR 转 SDR 映射；它只是在不应用 Gain Map 的情况下显示 Base Image。对于查看基础 SDR 内容，这正是合理且快速的路径；对于模拟系统如何把 HDR 压回 SDR，则需要另一个明确标记的 tone mapping 路径。

当前显示路径改为优先使用 ImageIO 的原生解码选项：

- DecodeToSDR：读取基础 SDR 结果。
- DecodeToHDR：让系统和 ImageIO 按图片元数据生成 HDR 结果。

同时对 SDR、HDR 分别缓存，避免 SDR 切回 SDR 时再次触发完整解码。显示切换只改变当前展示缓存和分析域，不再隐式改变图片源数据。

### 2.3 Gain Map 信息模块

用户提供了 Gainmap Demo App 的信息截图，之后补充了以下信息：

- API：Metal。
- GPU view 是否 overrange。
- HDR 屏幕 max、potential、reference 和 headroom。
- Gain Map minimum version、writer version、是否 multichannel。
- 是否使用基础色彩空间。
- Base HDR Headroom、Alt HDR Headroom。
- Base Color Space、Alternate Color Space。
- GainMapMin、GainMapMax、Gamma、BaseOffset、AlternateOffset。
- Gain Map 类型、分辨率、缩放比例和 Gain Map Weight。

界面先经历了“图片信息中混合展示”方案，后来拆为 Gain Map 优先的信息区域，并最终形成三部分：

1. API 和显示环境：API、GPU/屏幕能力、overrange 和屏幕 headroom。
2. HDR 核心参数：HDR Headroom、Alt HDR Headroom、GainMapMax、GainMapMin。
3. 其余 Gain Map 元数据：版本、通道、色彩空间、传输函数、Gamma、Offset、类型、尺寸、权重和像素格式。

这里的 HDR Headroom 不是简单的“照片最大亮度”。它通常表示基础图和备用 HDR 表达之间的相对曝光/动态范围关系。Alt HDR Headroom 是 Alternate 表达对应的头顶空间，GainMapMax/GainMapMin 是 Gain Map 对数增益范围。三者有关联，但不能互相替代：

- Headroom 描述允许或声明的整体范围。
- GainMapMin/Max 描述局部 Gain Map 数值可能落入的增益边界。
- Alternate 色彩空间和传输函数决定这些增益最终应如何解释。

因此，显示面板必须同时展示原始字段和“推断/计算字段”，不能把 Headroom 直接标成绝对峰值 nits。

### 2.4 基础色彩空间与 Alternate 色彩空间

对样片的分析确认了一个关键事实：主图或 Base Image 可以是 Display P3，而 Gain Map 的 Alternate 表达可以是 BT.2100 PQ；也可能是 Display P3 + PQ、自适应 Gain Curve，甚至 Base 和 Alternate 都是同一色域但传输函数不同。

这两个字段的意义不同：

- 主图色彩空间：主图文件和基础 SDR 表达使用的色域/编码约定。
- Gain Map 基础色彩空间：Gain Map 公式中的 Base 表达应按什么色彩空间解释。
- Gain Map Alternate 色彩空间：应用 Gain Map 后的 HDR/备用表达应按什么色彩空间和传输函数解释。

因此不能看到 BT.2100 就把整张照片标成 BT.2100，也不能只读取 ICC 后忽略 Gain Map auxiliary data。ICC 通常负责主图的颜色管理；Gain Map 的 Base/Alternate 约定来自 Gain Map 相关元数据，两者可能存在于不同的元数据块中。

当前代码已经把以下概念分开解析和展示：

- Base Color Space。
- Alternate Color Space。
- Base Transfer Function。
- Alternate Transfer Function。
- Pixel Format。
- Gain Map 类型和辅助图尺寸。

当前仍有一个明确的技术风险：显示和分析的源驱动重建主要在扩展线性 Display P3 缓冲区中完成，虽然能正确使用样片中的 Gain Map 数值，但要做到严格的 BT.2020/PQ 或 Display P3/PQ 色彩管理，还需要在 Alternate 域做显式颜色转换和传输函数编码。这个风险已经列入后续 P0，而不能用“把标签改成 BT.2100”来掩盖。

### 2.5 从显示重建转向照片本身的 HDR 分析

用户指出：HDR 效果分析不能只分析系统重建后的预览效果，否则结果会混入当前 Mac 屏幕能力、系统 tone mapping 和显示环境。

现在的原则是把三种东西分开：

1. 图片本身的源 HDR：Base Image + 原始 Gain Map + Gain Map 元数据。
2. 系统渲染预览：ImageIO/Core Image/系统显示路径生成的可见结果。
3. 当前屏幕可显示的范围：屏幕 headroom、过驱能力和 unsupported 部分。

HDR 效果分析优先针对第一项。系统预览可作为另一个上下文供对比，但不应冒充图片源的绝对 HDR 真值。

当前源驱动路径支持并验证了多种辅助 Gain Map 像素格式：

- L008。
- L016。
- 444f。
- 420f。

对于部分格式，代码先尝试直接读取 auxiliary data；如果系统提供了可用的 auxiliaryHDRGainMap，则使用 Core Image 兼容路径。应用不会把 fallback 的显示结果写成“已直接读取原始 Gain Map”，而是通过 sourceDriven/来源信息区分路径。

## 3. 核心维度的尝试方案、优缺点与当前结论

### 3.1 解码、缓存与性能

| 方案 | 优点 | 缺点 | 当前结论 |
| --- | --- | --- | --- |
| 每次切换都手工读取、计算并生成全尺寸图 | 公式可控，便于早期验证 Gain Map | 慢、重复分配内存，容易阻塞 UI，色彩处理风险高 | 已放弃 |
| 只隐藏 Gain Map 显示 Base Image | 几乎瞬时，适合查看基础 SDR | 不是 HDR 压缩到 SDR 的 tone mapping；会丢失 HDR 增强信息 | 作为 SDR 基础显示路径保留 |
| ImageIO DecodeToSDR/DecodeToHDR | 使用系统原生图像解码和元数据处理，结果更接近系统 | 具体实现受系统版本和图像格式影响，黑盒程度较高 | 作为显示首选 |
| SDR/HDR 两套独立缓存 | 切换时不重复解码，逻辑清晰 | 占用更多内存，需要淘汰策略 | 当前实现 |
| 预览优先、全尺寸异步分析 | 首屏更快，UI 不被分析阻塞 | 切换很快时仍可能产生过期任务和竞争 | 当前实现，需加强取消 |
| 每张图片都立即生成全尺寸 HDR、直方图、人脸和指标 | 结果完整 | 首次导入和批量切换慢，峰值内存高 | 已放弃作为默认策略 |

当前慢的根因不再只是“Gain Map 公式慢”，而是以下工作叠加：大图解码、颜色转换、辅助图展开、全尺寸统计、人脸检测、SwiftUI 状态刷新和缓存未完全淘汰。后续应建立性能预算，而不是继续对单个按钮加局部优化。

建议的性能预算：

- 缩略图首屏：优先显示预览，不能等待全尺寸分析。
- 单图切换：先显示已有预览或缓存，再后台补齐指标。
- HDR/SDR 切换：已有缓存时只切换引用，不重新解码。
- 直方图：优先使用缩略图或固定采样缓冲，只有用户请求精确分析时才升级到全尺寸。
- 人脸检测：按需执行，并支持任务取消。

### 3.2 HDR/SDR 显示交互

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| SDR/HDR 选择器 | 表达清晰，适合原型阶段 | 操作成本高，用户需要在选择控件中切换 |
| B 键快速切换 | 快，适合熟练用户 | 可发现性弱，焦点不在主图时可能无效 |
| 同一 Tab 点击切换 | 与浏览器/查看器习惯一致，状态集中 | 需要清楚显示当前模式，否则容易误判 |
| 独立 SDR/HDR Tab + 当前模式状态 | 视觉最明确，可承载独立直方图和分析 | 需要严格定义每个 Tab 的数据域 |

当前采用快捷键 B 和模式状态结合的方式。后续建议将模式切换提升为明确的主视图状态机：

- SDR：显示 Base/SDR 结果，直方图统计 SDR 域。
- HDR：显示源 HDR 或系统 HDR 预览，直方图和效果分析显示对应来源。
- Preview/Source 标签：明确用户看到的是系统渲染还是源驱动分析。

关键规则是：模式变化必须同时刷新主图、直方图、亮度统计、效果分析和肤色分析；不能只替换图片而保留旧模式的统计结果。

### 3.3 色彩空间和传输函数

曾经尝试过以下简化：

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| 只读主图 ICC | 简单，普通照片兼容性好 | 看不到 Gain Map Alternate，HDR 解释不完整 |
| 发现 HDR 就统一标 BT.2100 | 展示直观 | 会把 P3 Base、P3+PQ 和 BT.2020/PQ 混为一谈，映射可能错误 |
| 只展示 P3，不处理 auxiliary data | 颜色标签简单 | HDR 视觉效果和 HDR 指标缺失 |
| 分开展示 Base/Alternate/Transfer Function | 语义正确，便于调试 | UI 信息量大，需要说明关系 |

当前结论：第三种方案不够，第四种方案是正确的长期方向。必须保留原始字段、来源和置信度，并在严格渲染时执行：

1. Base 颜色空间解码到规范的线性工作空间。
2. Gain Map 增益按 ISO 参数、Gamma、Min/Max、Offset 和 Weight 还原。
3. 结果转换到 Alternate 色彩空间。
4. 按 Alternate 传输函数编码或交给系统色彩管理显示。

### 3.4 直方图

直方图经历过“直接对显示截图统计”“SDR/HDR 共用 RGB 统计”“按 UI 左右区域模拟 Gain Map”“把系统预览当作源 HDR”几类思路。这些方案都容易造成用户看到的直方图与照片实际数据不一致。

当前方向参考 Adobe/Gainmap Demo 的信息分区，但必须先把统计域定义清楚：

| 统计域 | 统计对象 | 适用问题 |
| --- | --- | --- |
| SDR Base | Base Image 经正确颜色解码后的 SDR 亮度/通道 | 基础 SDR 曝光、黑白场、通道剪裁 |
| Source HDR | Base + 原始 Gain Map 形成的源 HDR 亮度/通道 | HDR 扩展、Gain Map 增益和源高光 |
| System Preview | 当前 ImageIO/Core Image/系统 HDR 预览 | 当前设备实际看到什么 |
| Screen Capability | 屏幕 headroom 和可显示上限 | 哪些 HDR 档位能显示，哪些会被压缩/隐藏 |

当前直方图已独立放在右侧 Gain Map/照片信息面板上方，默认展开，并可收起；但它仍需要进一步完成以下工作：

- 直方图横轴使用统一的可解释域，不把 SDR 0-1 和 HDR EV 直接拼成一个没有标注的坐标。
- RGB 曲线、亮度曲线、剪裁标记和 Gain Map HDR 分区分层绘制。
- HDR 区域的虚线档位必须来自 headroom 档位计算，而不是固定像素分割。
- 底部能力条要区分“图片拥有的 HDR 范围”和“当前屏幕支持的范围”。
- unsupported 部分可以灰显或隐藏，但不能从源统计中删除。
- 模式切换时清空/替换统计缓存，避免 SDR 图显示 HDR 直方图。
- 使用固定采样和颜色管理后的像素域，避免不同分辨率导致直方图形状变化。

后续验收应使用同一组样片，在 SDR、源 HDR、系统预览三个域分别导出统计数据，确保 UI 曲线与原始数组一致。

### 3.5 单图查看、缩放、平移和边界

最初允许图片自由拖到任意区域，结果是图片可以完全移出视口、遮挡工具栏，且多图放大后互相重叠。之后加入了缩放边界、平移约束和对比布局，但仍应按 XnView 类查看器的交互逻辑做一次系统化重构。

当前保留的正确方向：

- 缩略图、单图、对比是不同工作模式。
- 鼠标滚动在主图区域用于缩放，而不是上下滚动图片。
- 图片平移不能把有效内容全部拖出视口。
- 单图和多图各自拥有 viewport 状态。
- 对比布局支持自动对齐和独立缩放/平移。

当前主要文件：

- ImageCanvas.swift：画布、手势和显示层。
- ViewportState.swift：缩放、平移和边界状态。
- ContentView.swift：浏览器/查看器/对比模式组织。
- PhotoStore.swift：当前图片、选择和导航状态。

后续建议将交互重构为显式状态机：

    Browser -> Viewer -> Compare
    Browser + selection -> Compare
    Viewer + next/previous -> Viewer
    Compare + next/previous -> Compare

每个模式定义自己的状态：

- viewportScale：当前缩放。
- viewportOffset：当前平移。
- fitMode：适合窗口、适合宽度、100%、实际像素。
- compareLayout：水平、垂直、网格和分割线。
- focusPane：对比时当前键盘焦点。
- imageBounds：图片在视口中的可见边界。

平移约束不能只限制 offset 数值，还要根据图片尺寸、视口尺寸、缩放比例动态计算最小/最大范围。窗口变化、切图、旋转、布局变化后必须重新 clamp。缩放应以鼠标位置或视口中心为锚点，避免用户放大后目标跳走。

### 3.6 缩略图选择、导航和快捷键

已实现或已约定的交互：

- Shift 点击：选择锚点到当前项目之间的连续范围。
- Enter：选中一张或多张图时进入图片对比。
- B：在 SDR/HDR 间切换。
- 缩略图中的上一张/下一张：只切换当前缩略图选择，不自动跳到大图模式。

这个方向保留了 XnView 类工具的效率，但仍需要补齐：

- 明确“当前项目”和“选中集合”的区别。
- 保留 range anchor，避免每次点击后 Shift 范围失效。
- 键盘焦点在缩略图、主图和对比 pane 之间切换时保持一致。
- 当前选择变化、文件排序变化和异步加载完成时，避免 index 错位。
- 文件删除、过滤、排序和重新扫描时，选择应按稳定 URL/ID 恢复，而不是只按数组下标。
- 在没有可用 HDR 数据时，B 键应给出明确状态反馈。

### 3.7 Gain Map/照片信息面板

当前信息展示重点已经从“只有普通 EXIF”转向“Gain Map 优先，同时保留照片信息”。面板应至少区分三类证据：

1. 文件和照片基础信息：文件名、格式、尺寸、方向、文件大小、创建/修改时间、相机和镜头。
2. 拍摄和 EXIF 信息：拍摄时间、曝光时间、光圈、ISO、焦距、测光模式、曝光程序、白平衡、闪光灯、场景/拍摄模式、GPS（若存在）。
3. Gain Map 和显示信息：Base/Alternate 色彩空间、传输函数、Headroom、增益范围、Gamma、Offset、辅助图格式、尺寸、权重、API 和屏幕能力。

“环境亮度”需要特别谨慎。普通 JPEG/HEIC 的 EXIF 并不一定有可验证的环境照度；如果没有传感器或厂商私有字段，只能显示“未提供”，不能根据照片亮度猜出真实 lux。拍摄模式也要区分 EXIF 原始值、厂商 MakerNote 值和应用推断值。

后续应引入字段来源和质量标记：

- EXIF：文件明确提供。
- XMP/MakerNote：厂商或软件扩展提供。
- GainMap auxiliary metadata：辅助图规范提供。
- Derived：应用根据像素计算。
- Unavailable：没有可靠数据。

### 3.8 效果分析

效果分析最初作为信息 Tab 的一部分，后来移动到工具栏，并拆分为 SDR 效果和 HDR 效果。当前采用的原则是：

- SDR 分析回答基础图的曝光、对比、黑白场、色彩分布和细节情况。
- HDR 分析回答照片本身的 Gain Map 扩展、HDR 头顶空间、局部增益、高光扩展和显示支持情况。
- 分析结果必须注明是源数据、系统预览还是屏幕能力。
- 指标要能由像素或元数据复算，不能只写主观评价。

当前已使用/规划的有效指标包括：

SDR：

- 线性亮度分位数：P1、P5、P50、P95、P99。
- 黑位占比、白位占比、通道剪裁占比。
- 动态范围、局部对比度和亮度标准差。
- RGB 通道偏差、饱和度分布、色相分布。
- 高频/低频细节比例和清晰度代理指标。

HDR：

- Gain Map Min/Max 与实际增益分位数。
- HDR 增益覆盖比例和高增益像素比例。
- 源 HDR 的亮度分位数及相对 SDR 的 EV 扩展。
- 高光扩展区、压缩区、屏幕 unsupported 区的比例。
- Base 与 Alternate 色彩空间/传输函数。
- Gain Map Weight、辅助图分辨率和重建覆盖率。

当前指标主要表达相对曝光/增益和统计分布，不等同于绝对 nits。只有当输入明确携带可靠的 HDR 标定和显示变换时，才可以将结果报告为绝对亮度。下一步应把每一个数值配上“统计域、采样数、单位、来源、置信度”。

### 3.9 肤色指示器和人脸分析

肤色指示器参考了 DaVinci Resolve 的肤色线/矢量示波器理念，以及 Adobe 对人像颜色观察的实际需求。已尝试过：

- 全图肤色候选点：覆盖全面，但背景、木材、砖墙和暖色灯光容易误报。
- HSV/YCbCr 规则：速度快、可解释，但不同肤色、白平衡和光照下阈值敏感。
- 静态肤色线：便于快速判断色相偏移，但不代表“正确肤色”，也不能替代人脸识别。
- Vision 人脸检测后只统计脸部区域：误报少，更贴近人物肤色，但侧脸、遮挡、小脸和低光下可能漏检。

当前方向是自动进行人脸检测，不再要求用户打开单独开关；检测完成后，以人脸区域为主要统计域，同时保留全图肤色候选作为参考。UI 应同时呈现：

- 人脸数量、有效脸部像素数量和检测置信度。
- 每张脸的平均/中位肤色色相、饱和度、亮度。
- 相对于肤色参考线的色相偏差。
- 不同人脸之间的色差和曝光差异。
- SDR/HDR 模式下肤色统计是否来自 Base、源 HDR 还是系统预览。

后续要避免把肤色线当成审美评分，也不能根据肤色指标直接判定照片好坏；它主要用于检查色相偏移、肤色不一致、过饱和和曝光不足。

## 4. 当前代码结构与职责

### Models.swift

定义显示模式、Inspector Tab、效果分析域、HDR 信息、Gain Map 信息、照片元数据、分析结果和工程文档模型。后续需要为分析来源、统计域和字段来源增加显式枚举，逐步减少 Bool 表达复杂状态。

### ImagePipeline.swift

负责：

- ImageIO 图片加载和缩略图生成。
- DecodeToSDR/DecodeToHDR 显示解码。
- ICC、Base/Alternate 色彩空间和传输函数识别。
- Gain Map auxiliary data 读取和 Core Image fallback。
- ISO 21496-1 Gain Map 参数解析。
- SDR、HDR 和人脸分析采样。

这里是整个产品最重要的正确性边界。任何颜色、HDR、Gain Map 或直方图修复，都必须先在此处明确数据域和颜色变换，再改 UI。

### PhotoStore.swift

负责：

- 文件夹扫描、排序和选择。
- Shift 范围选择。
- 当前图片和对比集合。
- 预览、解码和分析缓存。
- 上一张/下一张导航。
- 工程保存/打开。

后续建议把任务取消、LRU 缓存、预取和稳定 ID 都集中在这里管理。

### ContentView.swift、ImageCanvas.swift、ViewportState.swift

负责浏览器、查看器、对比模式、主图画布、缩放、平移、边界和快捷键。交互重构应优先在这组文件中完成，避免继续向各个按钮增加互相独立的边界修补。

### HistogramView.swift、EffectAnalysisViews.swift、AnalysisViews.swift

负责直方图、工具栏中的效果分析、Gain Map 信息和肤色/人脸可视化。后续应改为接收明确的数据对象和统计域，避免视图自行猜测 SDR/HDR 模式。

## 5. 已验证样片与回归基线

此前对一组来自 TECNO/CN7C 的样片和竞品 Gain Map 样本做过解析。重要结果如下：

| 样片 | 主图/Base | Alternate | Gain Map/格式 | Headroom |
| --- | --- | --- | --- | --- |
| IMG20260811154350.jpg | Display P3 | Display P3 + PQ | 444f | 2.300450 |
| IMG20260811154414.jpg | Display P3 | BT.2100 PQ | L008 | 2.300450 |
| IMG_20260811_154318.jpg | sRGB EOTF with DCI-P3 Color Gamut | 同一 Base/Alternate 约定 | 420f | 2.148445 |
| IMG_20260811_154504_359.jpg | Display P3 | BT.2100 PQ | L008 | 2.232660 |
| IMG_2258.HEIC | Display P3 | Display P3 + PQ，带 Adaptive Gain Curve | L008 | 2.431641 |

修复后，这五个样片都能进入 source-driven 分析路径，分析采样数达到 786432/786432。这个结果只能证明当前格式覆盖和采样链路已打通，不代表所有相机、所有厂商私有 JPEG/HEIC 都已严格正确。

样片不应直接提交到源码仓库。新电脑迁移时，应把样片作为独立测试资产放入本地测试目录，并在手稿或 README 中记录路径、SHA-256 和预期字段。若未来建立受控回归集，建议只提交脱敏、获授权的样片或其元数据 golden 文件。

## 6. 当前未完成问题与风险分级

### P0：正确性和可验证性

1. 完成严格的 Base 到 Alternate 色彩空间转换，尤其是 BT.2100/PQ、Display P3/PQ 和不同传输函数组合。
2. 为直方图明确 SDR Base、源 HDR、系统预览、屏幕能力四个统计域，并为每个域建立 golden 数据。
3. 将 sourceDriven Bool 升级为显式来源枚举，例如 directRawGainMap、coreImageAuxiliaryGainMap、systemDecode、baseOnly、unavailable。
4. 明确 Headroom、GainMapMin/Max、Alternate 传输函数之间的公式关系，避免把相对值展示成绝对亮度。
5. 对更多真实设备样片做交叉验证：Gainmap Demo、系统预览、ImageIO、Core Image 和本软件结果逐项比对。

### P1：体验和性能

1. 重构 Viewer/Compare 的 viewport 状态机和边界约束。
2. 加入缩略图预取、LRU 缓存、任务取消和按需全尺寸分析。
3. 统一切图、选择、焦点和键盘事件处理。
4. 直方图支持固定采样、分位数标记、剪裁区和 HDR 档位能力条。
5. 人脸检测和效果分析任务支持取消、进度和结果来源显示。

### P2：专业能力和发布

1. 扩展 EXIF、XMP、MakerNote 字段，并标注字段来源。
2. 完善肤色矢量示波器的颜色管理、参考线和多脸对比。
3. 增加导出分析报告、CSV/JSON 指标和截图标注。
4. 增加单元测试、样片回归测试、性能基准和内存峰值记录。
5. 形成 Developer ID 签名、公证、版本号、更新说明和 universal binary 发布流程。

## 7. 建议的后续开发顺序

### Milestone 1：冻结数据语义

- 先定义 ImageSource、AnalysisDomain、HistogramDomain、ColorProvenance。
- 为所有指标增加单位、采样数、来源和置信度。
- 不改 UI 视觉，先让模型能表达“源 HDR”和“系统预览”的区别。

### Milestone 2：完成颜色和 HDR 正确性

- 以五个现有样片为最小回归集。
- 加入 BT.2100/PQ、Display P3/PQ 和 Base-only 对照。
- 对比 Gainmap Demo 和系统显示结果。
- 把不能确认的字段显示为未知，不做猜测。

### Milestone 3：重构直方图和效果分析

- 统一采样器和颜色转换入口。
- SDR/HDR 分别统计，直方图 UI 只负责呈现。
- HDR 区域由实际 Headroom 档位计算。
- 将屏幕支持范围画成能力层，不修改源 HDR 统计。

### Milestone 4：重构 Viewer/Compare 交互

- 建立 Browser、Viewer、Compare 状态机。
- 统一缩放锚点、边界 clamp、Fit/100%/实际像素。
- 对比时提供独立 pane 状态、同步缩放开关和自动对齐。
- 用键盘焦点和稳定图片 ID替代散落的 index 判断。

### Milestone 5：性能和发行

- 建立冷启动、热切图、HDR 切换、直方图、人脸分析的基准。
- 预览先行，后台补齐；所有异步任务可取消。
- 检查内存峰值和缓存淘汰。
- 最后再做签名、公证和版本发布。

## 8. 新电脑迁移和日常工作流

新 Mac 建议安装：

- macOS 14 或更高版本。
- Xcode 和对应 Command Line Tools。
- Apple Silicon 环境优先。
- GitHub CLI gh，并登录拥有该 private repo 权限的账号。

首次迁移：

    gh auth login
    git clone https://github.com/owspace/One-IMAGE-Compare-1.0.0-source.git
    cd One-IMAGE-Compare-1.0.0-source
    ./scripts/verify_structure.sh
    swift build -c debug

生成内部 arm64 DMG：

    ./scripts/build_dmg.sh

日常开发建议：

1. 每次开始工作先阅读本手稿和 README。
2. 先运行 git status -sb，确认没有覆盖新电脑上的本地改动。
3. 一个主题一个提交，例如颜色语义、直方图、交互、性能分别提交。
4. 修改后至少运行结构校验和 Debug 构建；涉及图像管线时运行样片回归。
5. 不把 DMG、DerivedData、.build、测试照片和个人路径提交到仓库。
6. 提交前写清楚“显示域、分析域、来源”和已知限制。

建议的新功能分支命名：

    codex/color-provenance
    codex/histogram-domains
    codex/viewport-state-machine
    codex/performance-cache

## 9. 设计和开发判断总表

当前最重要的四条判断是：

    显示结果 ≠ 图片源内容
    元数据证据 ≠ 完整渲染管线
    相对分析指标 ≠ 绝对亮度真值
    屏幕支持范围 ≠ 图片拥有的 HDR 范围

这四条判断应该贯穿后续所有 UI、指标和代码命名。尤其是 HDR：

- ImageIO/Core Image 的结果适合回答“系统如何显示”。
- Base + Gain Map + 元数据适合回答“照片本身有什么 HDR 扩展”。
- 屏幕 headroom 适合回答“当前设备能显示多少”。
- 直方图和效果分析必须明确自己回答的是哪一个问题。

后续开发的最高优先级不是继续增加信息字段，而是把颜色管理、统计域、来源标记和交互状态机做成稳定基础。基础正确后，再扩展更多 EXIF、专业指标、肤色可视化和发布能力，产品才不会因为显示环境变化而给出看似精确但语义错误的结论。

## 10. 当前交接结论

项目已经从一个能打开图片的原型，发展为具备浏览、对比、Gain Map 解析、HDR/SDR 显示和效果分析骨架的 macOS 工具。已解决的问题主要集中在：

- 原有构建和打包链路。
- HDR/SDR 切换的重复解码。
- Gain Map 信息缺失和面板组织。
- Base/Alternate 色彩空间的语义区分。
- 多种辅助 Gain Map 像素格式的读取兼容。
- Shift 选取、快捷键和基础视图边界。

尚未完成的核心问题主要集中在：

- 严格的 Alternate 色彩空间渲染。
- Adobe/Gainmap Demo 级别的多域直方图。
- XnView 级别的查看器/对比器状态机。
- 可取消、可预取、可量化的性能体系。
- 由样片 golden 数据支撑的回归验证。

接下来任何重大修改，都应先在本手稿中更新“数据语义、方案取舍或风险等级”，再进入代码实现。这样新电脑上的 Codex 才能沿着已经验证的方向继续，而不会重新走回手工 HDR 映射、混合统计域或自由拖拽导致的旧问题。
